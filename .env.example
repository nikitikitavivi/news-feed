# Docker Compose — local development only
# Copy this to .env and fill in real values (or leave defaults for local dev)

POSTGRES_USER=newsfeed
POSTGRES_PASSWORD=change-me-in-production
POSTGRES_DB=newsfeed
